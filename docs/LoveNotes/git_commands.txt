$ git config --global user.name "adyliviu"
$ git init
$ git config user.email "adyliviu@adyemailserver.local"
$ git config --list
$ git help
$ git help commit
$ pwd
$ cd ~
$ cd ..
$ ls
$ cd adria
$ cd /e
$ git add .
$ git commit -m "This is out first commit"
$ git log
$ git log --author="adyliviu"
$ git status
$ git add dummyfile.txt
--not a command -- $ working copy > staging area > repository
$ cd docs
$ git commit -a -m "commited all"
$ git diff --staged
$ git rm dummyfile.txt
$ git commit -m "deleted dummy file"^C
$ git status
$ git add git_commands.txt
$ git rm "git commands.txt"
$ git status
$ git commit -m "renamed git commands.txt to git_commands.txt"
$ git mv git_commands.txt gitcommands.txt
$ git mv gitcommands.txt LoveNotes/git_commands.txt
$ git status
$ git commit -m "renamed file and put in LoveNotes folder"
$ git commit -am "cleaned up the comments"


